STATE: SOURCE_READY

OWNED FILES CHANGED:
- `Plugins/QSystem/Source/QSystem/Public/Component/QPlayerControllerInteractComponent.h`
- `Plugins/QSystem/Source/QSystem/Private/Component/QPlayerControllerInteractComponent.cpp`
- `Plugins/QAutomatedTestSuite/Source/QAutomatedTestSuite/Private/QPlayerControllerInteractionTests.cpp`
- `Documentation/BlueprintToCppMigration/02_PLAYER_INTERACTION_MIGRATION.md`
- `Documentation/BlueprintToCppMigration/HANDOFFS/08_INTERACTION_PACKAGED_PARITY_AUDIT.md`

EVIDENCE:
- Latest installed packaged Development evidence is `C:\Program Files (x86)\Steam\steamapps\common\Qanga\Qanga\Saved\Logs\QANGA.log`. It identifies `Build Configuration: Development` and the installed binary; `Qanga.exe` is dated `2026-08-28 18:58:38`.
- That binary predates the event-driven tracker/prompt commit `90f0ba1e4` (`2026-08-28 19:55:22 +02:00`) and pilot-hierarchy/label cleanup commit `0d57d4dc8` (`2026-08-28 23:14:42 +02:00`). It cannot validate those fixes or current packaged parity.
- The installed Development log has no `LogQPlayerInteraction` event. This does not prove that any interaction scenario ran. It does show `W_TrackerElem.WarpOcclusionUpdate` retaining a pending-kill `TrackerComponent` every roughly `0.5 s` from `17:39:05.456` through `17:39:08.518`; the log carries no tracker business identity, so this proves a tracker lifecycle defect in that old build but does not attribute it to the current interaction prompt. At `17:39:07.351` it separately reports a null `FloorComponent` in `BP_Elevator.SetInteractionEnabled`.
- The latest local client log, `C:\Users\Rz\AppData\Local\Qanga\Saved\Logs\QANGA.log`, is Shipping from `2026-08-27 01:34:31`, before all but the first interaction repair. It has no current-parity evidence.
- Historical Development Editor evidence in `Saved/Crashes/UECC-Windows-8F2694D94EA3733A13506D9D19A7E868_0000/QANGA.log` directly records the old failure mechanism: `MainDoor` server-query refusal at `16:12:24.041`; duplicate `Driver` presses from `16:12:30.936` through `16:12:46.677`; a release with no authorized target/id at `16:12:35.523`; repeated `MiddleDoor` presses from `16:12:54.861` through `16:13:02.321`; and duplicate `Driver` presses on a second vehicle at `16:13:48.599` / `16:13:49.749`. This log predates `976971f95`, `495d08110`, `10d984fdc`, `90f0ba1e4`, and `0d57d4dc8`; it proves the historical symptoms, not their current repair.
- The current `2026-08-30` Editor logs contain no `LogQPlayerInteraction` entries. Successful focus, possession, prompt, and dispatch transitions are not logged, so their absence cannot be correlated or used as a parity claim.
- Current native architecture has one detection/focus owner (`UQPlayerControllerInteractComponent`) and one authoritative server funnel (`SV_Interact` -> `RunServerValidationQuery` -> `ResolveInteractionForView` / `DoesResultAuthorizeTarget` -> `DispatchInteractServer`). Client and server consume the same candidate ranking, vehicle eligibility, component-exact occlusion, and target-authorization decisions. The exact active hold target/id is the only release path allowed to bypass a fresh view query, so a legitimate release survives focus or possession loss without authorizing a new target.
- Hot-path policy is intact: one polled detection deadline at `1 / 30 s`, ignore-list refresh at `1 s`, failure logs throttled to `1 s`, no catch-up loop, no stable-focus presentation heartbeat, and detection tick enabled only for a local controller. Dedicated-server and non-owner controller copies do not run detection; a listen host remains the locally controlled owner.
- Read-only live Blueprint inspection found no competing detector in `/Game/Systems/Interact/PlayerControllerInteractComponent`: `InteractInput` resolves the native exact input target, while `AfterCheck` only drives highlight/hold/prompt presentation. `/Game/Systems/Vehicle/VehicleBase` rejects a repeated Press while `bActiveHoldInteraction` is set; release sends `ActiveHoldInteractionId`, clears the active flag/id, and rearms only after cleanup.
- Read-only widget inspection found the intended event-driven contract: `W_InteractFeedback.BPI_TickEnableInteractText` uses a zero-lifetime persistent temp tracker and destroys/collapses it on invalid target; `BPI_SetIsHoldInteract` immediately rebuilds the prompt text. `Content/Widget/HUD/Composition/W_InteractFeedback.uasset` is currently modified by another peer and was not touched here.
- Current source explains the six reported symptom classes but does not yet prove their packaged result: stateless non-hold vs paired holds and VehicleBase repeat gating address repeated presses; explicit vehicle-exit routing plus exact input/hold target memory address intermittent exit; event-driven persistent tracker ownership addresses blink/stutter; immediate prompt rebuild addresses stale Press/Hold text; pilot hierarchy eligibility addresses prompts during active piloting; component-exact local occlusion plus identical server revalidation addresses through-wall component use.
- One current native defect was proven. `GetInteractionDispatchTarget()` still returned `ActiveInteractActor` when the exact accepting object became invalid before the next `30 Hz` poll. It also allowed a sit interaction to retain its actor if the required socket component disappeared. This was the silent component/source-to-actor downgrade that commit `10d984fdc` did not fully remove.
- The smallest repair removes `ActiveInteractActor` from dispatch resolution. A new pure decision requires the exact accepting object and any required physical source to remain valid before selecting the custom target or exact target. No grace, threshold, fallback, asset exception, or second owner was added.
- `QATS.QSystem.PlayerInteraction.InputTargetResolution` now deterministically covers custom-target priority, exact-target selection without a custom target, exact-target loss, and required-source loss. The remaining three QATS tests and eight adjacent `QSystem.Interaction.*` tests already cover stateless vs hold disposition, exact release pairing, runtime unavailability, focus grace/context transitions, pilot/passenger/exit eligibility, exact occlusion, authorization, deadline cadence, and presentation notifications.
- No build, automation test, Editor command, PIE, asset mutation/save, cook, package, EasyCook rescan, stage, or commit was performed by this lane.

INTEGRATION REQUESTS:
- The owner of the concurrent `W_InteractFeedback.uasset` change must preserve and save/compile the inspected contract: one tracker creation/attachment per focus acquisition or replacement, lifetime `0`, explicit cleanup on loss, and immediate `ApplyPromptText` from `BPI_SetIsHoldInteract`. Do not restore a `30 Hz` heartbeat or temporary lease.
- No VehicleBase graph change is requested. Preserve the current `bActiveHoldInteraction` Press gate and exact `ActiveHoldInteractionId` release cleanup when integrating other asset work.
- Recheck the `W_TrackerElem` pending-kill warning only in a fresh build containing `90f0ba1e4` and `0d57d4dc8`. If it still reproduces, the tracker/widget owner must end the scheduled `WarpOcclusionUpdate` with tracker teardown (or tear down the widget with its tracker) instead of adding native interaction grace or recreation.
- Recheck `BP_Elevator.SetInteractionEnabled` with a current asset. If `FloorDoorBase_C.FloorComponent` is still null, the elevator owner must repair that initialization/lifetime boundary; it is independent of native interaction targeting and outside this lane.

TESTS FOR INTEGRATOR:
- First build `QangaEditor Win64 Development`, then run automation filters `QATS.QSystem.PlayerInteraction.*` and `QSystem.Interaction.*`. Expected inventory is four QATS tests and eight QSystem interaction tests, all green, including the new dispatch assertions.
- Editor Standalone, real production actors: repeat an instant actor/component/custom interaction at least ten times on the same target and ten times alternating two targets. Each Press must produce one effect; every subsequent Press must remain available; Up without a matching Down must do nothing.
- Editor Standalone, vehicle: hold the physical interaction key long enough to trigger OS/InputSystem repeats; enter Driver and passenger slots, release, exit, and repeat five full cycles. A completed hold must send one target/id pair, repeated Press events must not restart it, release must close that exact pair, and the next cycle must start normally. Also test tap/early release, focus loss during hold, and possession change during hold; cancellation must not activate, while an already authorized hold release must still close its original pair.
- Editor Standalone, transient possession bridge: during enter/exit possession, attempt a new Press and release an already latched action. New dispatch must remain blocked while runtime context is unavailable; the old release must retain its exact target; the first Press after context recovery must work without restarting the component.
- Editor Standalone, prompt lifecycle: alternate repeatedly between a Press target and a Hold target without pressing the key, then hold each focus stable while moving the camera and at low frame rate / a hitch longer than `150 ms`. The label must update on focus change, one prompt must track smoothly without blink/recreation, and loss/replacement/destruction/stream-out must remove the old prompt with no orphan or stale text.
- Editor Standalone, new stale-source regression: focus a component-only interaction and a QTrain sit socket, invalidate or destroy the exact component before the next detection poll, then Press. Dispatch resolution must be null, focus must clear, and neither actor fallback nor server effect may occur.
- Editor Standalone, piloting policy: while the possessed pawn is the vehicle, aim at the vehicle root, an attached button/door, and a child actor. None may show or dispatch a prompt. With no world focus, the dedicated exit input must still work. The same onboard target must remain available to a passenger and after exit; an unrelated world target must remain available while piloting.
- Editor Standalone plus server-forgery check: put a wall or a different component of the same actor between view and the accepting component. Local focus must reject it and a direct RPC request must be rejected by the server. Remove the blocker and verify the exact component/custom target succeeds. Repeat for an actor target to prove normal visibility still works.
- Editor network matrix: Listen Server with host plus remote client, then Dedicated Server plus two clients. Only each owning local controller may detect/present; server and remote copies must not tick detection. Repeat instant, hold/release across focus loss, vehicle exit/possession, pilot filtering, and occluded forged request from every owner.
- Editor business matrix: run QCableConnector, elevator, item pickup, vehicle enter/exit, quest objective, QTrain sit/stand, and the production cyborg hold. This matrix remains a release gate even if the generic tests pass.
- Fresh packaged Development only after the native change and the final saved widget/VehicleBase assets are included: cold boot with a new log and verify the executable is newer than all five interaction commits and this source repair. Repeat the Standalone instant/hold/vehicle/prompt/pilot/occlusion/stale-source/business scenarios with real OS key repeat and at least one low-FPS/hitch run; then repeat owner/server cases against a Development dedicated server.
- Packaged pass criteria: zero `LogQPlayerInteraction` rejection for valid actions; one observable gameplay effect per intended activation; no stuck active hold after Up/possession change; no vehicle-hierarchy prompt while piloting; exact occluded component requests rejected; no `W_TrackerElem` pending-kill warning; no stale Press/Hold text or tracker after target loss. Because failure logs are throttled to `1 Hz` and successful actions are not logged, validate counts from gameplay/server state rather than inferring them from log silence.

REMAINING:
- Compilation and both automation filters are unrun by design.
- Editor runtime replay after the native dispatch repair is open.
- A fresh packaged Development build containing `90f0ba1e4`, `0d57d4dc8`, the current assets, and this repair does not yet exist in the inspected locations.
- Packaged parity, tracker cleanup, pilot prompt suppression, label refresh, vehicle exit reliability, and the extended business matrix remain unproven until the scenarios above pass.
